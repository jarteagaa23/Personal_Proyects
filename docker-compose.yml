services:
  prueba:
    build: .
    ports:
      - "8080:8080"
    environment:
      - CHOCH=holamundo2
    
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mydb